<h1>Edit Reservation</h1>

<%= render "form.html",
  assigns
  |> Map.put(:action, Routes.reservation_path(@conn, :update, @reservation))
  |> Map.put(:selected_lanes, Enum.map(@reservation.lanes, &(&1.id)))
%>

<span><%= link "Back", to: Routes.reservation_path(@conn, :index) %></span>
